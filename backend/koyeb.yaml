name: thrivv-backend
runtime: nodejs
build_command: npm install
run_command: npm start
ports:
  - port: 5000
    protocol: http
env_vars:
  - key: PORT
    value: "5000"
  - key: NODE_ENV
    value: production
# Add MONGODB_URI and JWT_SECRET in Koyeb Dashboard
